name: Build & Deploy Angular (world → docs → GitHub Pages)

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses:actions/setup-node@v4
        with:
          node-version: 20

      # 👇 VERY IMPORTANT — run npm install INSIDE /world
      - name: Install dependencies
        working-directory: world
        run: npm install --no-audit --no-fund

      # 👇 ALSO IMPORTANT — run ng build INSIDE /world
      - name: Build Angular
        working-directory: world
        run: npm run build

      # 👇 Make 404.html INSIDE /world
      - name: Create 404.html
        working-directory: world
        run: cp docs/index.html docs/404.html

      # 👇 Upload the OUTPUT from /world/docs/
      - name: Upload docs folder
        uses: actions/upload-pages-artifact@v3
        with:
          path: world/docs/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
